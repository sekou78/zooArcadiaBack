<form method="post" action="{{ path('app_service_visite_petit_train_delete', {'id': service_visite_petit_train.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service_visite_petit_train.id) }}">
    <button class="btn">Delete</button>
</form>
